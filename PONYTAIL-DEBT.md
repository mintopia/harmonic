# Ponytail Debt Ledger

Every deliberate `ponytail:` shortcut in the repo, with its ceiling and upgrade
trigger. Generated by `/ponytail-debt`. Re-run the skill to refresh.

## src/daemon.ts

- `:51`, lock acquisition is a non-atomic read-then-write (two `serve`s on a fresh dir could both win). ceiling: fine for a local single-user tool (`start` is already shielded by its status check). upgrade: switch to an `O_EXCL` create with stale reclaim if simultaneous boots ever matter.

## src/tracker/github.ts

- `:132`, issue fetch caps at one page. ceiling: 1000 issues. upgrade: add `gh` pagination if a tracker outgrows one page.

## src/tracker/gitlab.ts

- `:109`, scan returns `comments: []` — skips per-issue notes. ceiling: scan omits comments (N+1, no scan consumer reads them). upgrade: `readTicket` fills them. `no-trigger`
- `:158`, issue fetch caps at 10 pages, matching the GitHub adapter. ceiling: 1000 issues. upgrade: none named. `no-trigger`

---

4 markers, 2 with no trigger.
